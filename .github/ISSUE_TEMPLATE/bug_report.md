---
name: Bug report
about: Something in repoboard did not behave as documented
labels: bug
---

**What you ran** — the exact command or UI action:

**What you expected:**

**What happened** — paste the output:

**`repoboard` version** — `node packages/server/dist/cli.js --version` (or `npx repoboard
--version` once it is published):

**OS and Node version:**

**Does `repoboard check` pass on your board?** (`node packages/server/dist/cli.js check`, or
`npx repoboard check`) — paste its output.
