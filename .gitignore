node_modules/
dist/
coverage/
*.tsbuildinfo

.env
.env.*
!.env.example

.claude/settings.local.json
.claude/settings.json
.claude/worktrees/
.claude/scheduled_tasks.lock

.DS_Store

# build / tool output
.vitest/

# repoboard runtime event log (ephemeral, see plan §2)
.repoboard/events.jsonl
.repoboard/local/

# RCB-121: prepack copies of the root README/LICENSE into the package dir (npm only takes them
# from there); never committed, regenerated by `npm pack`/`npm publish`'s prepack step.
packages/server/README.md
packages/server/LICENSE
