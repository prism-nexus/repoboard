---
id: RCB-111
title: "Flow view, no systems.yml: offer to create the cards that build the file — detect, hand-correct, connect — instead of only a note"
status: backlog
size: M
labels:
  - web
  - systems
refs:
  - packages/web/src/views/Flow.tsx
  - docs/SYSTEMS-FLOW-PLAN.md@3.2
created: 2026-09-22T17:46:22Z
updated: 2026-09-22T17:46:22Z
---
Owner, 2026-09-22, looking at ?repo=freshpickedjobs: 'the proposed yaml file for fpj — is there, when loading, the ask to create the tickets to create that file and do the mapping? it seems to currently be missing.'

Today: a repo without .repoboard/systems.yml shows only the one-line note ('no systems.yml yet; repoboard systems detect proposes one', Flow.tsx line ~361). Nothing on the page starts the work.

Proposed: in that empty state, a button 'Plan the systems map' that creates, on THAT repo's board (the one the page shows), one parent card 'Systems map for <repo>' with three steps (parent/phase/gate, RCB-68): PH.1 run systems detect and review the candidates (the dry run's numbers go in the body: n systems / m connections / k unclassified); PH.2 hand-correct and --apply (hand rows win, provenance stamped); PH.3 connections and the Flow view check. The click writes cards, so it is a deliberate click with the repo named in the confirm text, and it refuses when the repo has no .repoboard/ (map-only mode). The detect dry run behind PH.1 runs server-side on click (read-only) so the body carries real numbers from the start — fpj today: 14 / 13 / 7 (REFERENCE §7).

Needs: the existing POST /api/cards under /api/repos/<key>/ (repo-context.ts ~1062); a pure core function that builds the four cards, tested; the web button + confirm. Control: the empty state on a temp repo with no .repoboard/ shows no button.