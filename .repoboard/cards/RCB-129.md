---
id: RCB-129
title: No signal that an owner decision was answered (coordinator finding, fpj run day 2026-09-24)
status: todo
priority: medium
labels:
  - fpj-finding
created: 2026-09-24T18:44:25Z
updated: 2026-09-24T18:47:50Z
---

## Evidence

One of seven product findings the coordinator (freshpickedjobs-2c) sent at 2026-09-24 18:4xZ from
the fpj run day; recorded only as this one-line summary in the repoboard builder block of
.repoboard/local/log/2026-09-24.md (18:42:34Z). The full message was not kept. Before building,
ask the coordinator for the observed case, and reproduce it with a number. The seventh finding
(dist-stale warning on an npm install) was checked and not filed: dist-stale.ts skips when there is no src/, and 3 pack-smoke runs showed no warning.

## Log
- 2026-09-24T18:47:50Z repoboard builder — updated priority
## Observed case (coordinator fpj-2c, message 2026-09-24 ~18:5xZ)

FPJ-19 went to `decide` at 18:09Z (`card ask`, letters A–D, fpj b7dd7f3a). The owner answered B
on the web at 18:19:51Z. On disk: status decide → backlog and `chosen: B` in the card file,
uncommitted in the shared checkout. No log line, no STATE change, no stdout; the card just left the
generated OWNER QUEUE. The coordinator only saw it at 18:26Z because the card showed up in
`git status`. The builder learned about 10 min after the answer and typed the OWNER-DECISIONS row
by hand (fpj 64c4fda1, 18:31Z).

What the coordinator asked for:
- (a) a poll: `card list --decided-since <ts>` or a `decisions` subcommand listing card,
  question, chosen option, decided-at, decided-by;
- (b) `seat <name>` and `state` naming cards that are decided but not yet acknowledged (no
  `--as <assignee>` move/update after decided-at);
- (c) the web answer writing its own line (date, card, chosen text, "web") to a decisions ledger
  the seats already read.

Without (a), the only signal is a modified file in `git status`.

Coordinator's correction (2026-09-24 ~18:55Z): FPJ-19 was a MODIFIED tracked card, so RCB-119's
`untracked-cards` would NOT have named it. RCB-119 does not close this card. The cheap half: a
`modified-cards` check line (tracked card files with uncommitted changes, by actor). The
decided-since listing (a) is still what a seat needs.

Launch review item #24 (fpj coordinator, 2026-09-24 19:5xZ, at 99b9e76), the UI-side root cause:
DecisionSection (Drawer.tsx:341-360) and DecisionMark (CardItem.tsx:83-93) go quiet on purpose
once a card is decided ("D9: readability wins", Drawer.tsx:319).
