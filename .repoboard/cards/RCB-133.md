---
id: RCB-133
title: Lease and STATE section writes lose updates across processes — one lock primitive for the read-modify-write
status: done
priority: high
size: M
labels:
  - launch-review
  - tier1
created: 2026-09-24T19:49:10Z
updated: 2026-09-24T20:16:45Z
---

## Evidence

From the fpj coordinator's 0.2.0 launch review, 2026-09-24 19:5xZ, done read-only at 99b9e76. The owner's word, as relayed: "we want all of this fixed so we are best in class when folks start using it." Publish waits on the owner's say. Review items #1, #2, verbatim:

- **#1** **Lease take/release is a cross-process lost-update race** · store.ts:594-604 mutates `this.leasesDoc` (loaded once at openStore:429; the watcher reload at :1646 is off for CLI one-shots); tmp+rename (:1301-1309) makes each write atomic, not the read-modify-write · two `lease take` in the same second both succeed, the first holder's lease vanishes from disk · re-read + parse leases.yml inside `mutate` before computing (as setStateSection does at :658), or an O_EXCL `leases.yml.lock` around the span · M. (Coordinator re-verified by reading the code.)
- **#2** **STATE section writes: same race, smaller window** · store.ts:649-670 / 681-707 re-read fresh per call, but read(A) read(B) write(A) write(B) drops A's section · share the lock primitive with #1, or compare the stamp read vs on disk and retry · M (with #1).

The line numbers are as of 99b9e76; re-read them before briefing.

## Log
- 2026-09-24T20:16:45Z repoboard builder — moved todo → done
