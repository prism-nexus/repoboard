# P8.3 — STATE.md, the daily log, `init --practices`, `check` (brief, 2026-09-17)

Card: `RCB-38`. Plan: `docs/BUILD-PLAN.md` P8.3 and §11 O9. Builder subagent in
`/Users/hometown/Projects/Repos/Remember-Connect-Build` (no remote). **Starts AFTER P8.2 has landed**;
read `docs/P8.1-DECISIONS-BRIEF.md` §7 and `docs/P8.2-LEASES-WINDOWS-BRIEF.md` §7 first — they are the
newest record of the threading pattern and its corrections. Read `CLAUDE.md`, plan §0–§2 and P8,
`docs/AGENTS.md`, `/Users/hometown/Projects/Repos/freshpickedjobs/docs/VERIFICATION-SPECIES.md`. Move
`RCB-38` → `doing` (you as assignee) first; → `review` with a verification log line when done.

## Why this exists (measured)

freshpickedjobs' running record reached 23,877 lines because every seat appended to one file and
nobody rewrote a live-state page; a fresh seat needed 8 tool calls before its first useful sentence.
On 2026-09-17 it replaced that by hand with three files: a one-page STATE rewritten in place, a
per-day log every seat appends to, and the decisions file P8.1 built. The live shape is at
`/Users/hometown/Projects/Repos/freshpickedjobs/docs/STATE.md`, `docs/log/2026-09-17.md`,
`docs/NEXT-AGENT-PROMPT.md` — READ THEM (read-only) as the reference implementation.

## Locked decisions (build them; CORRECT in §7 with the measurement if one is wrong)

1. **`.repoboard/STATE.md`** — markdown, fixed H2 sections in this order: `## LIVE`, `## LAST LANDINGS`,
   `## OWNER QUEUE`, `## SEATS`. Line 1 is `# STATE`; line 3 is the stamp: `**Written <ISO> by
   <actor>.**`. Free text under LIVE / LAST LANDINGS / SEATS. **OWNER QUEUE is GENERATED** on every
   read from cards that NEED A DECISION (P8.1 / O10: `decision` block with no answer) — one line per card: `RCB-40 · <question> · [A B]`: the server renders it, and `repoboard state` prints it; the file
   itself keeps a one-line placeholder `_(generated from open decisions)_`. Rewritten in place, never
   appended. `repoboard state` prints the rendered page; `repoboard state --set-section LIVE --stdin`
   replaces one section's body and restamps line 3 (the only write surface besides an editor).
2. **`.repoboard/log/YYYY-MM-DD.md`** (UTC date). `repoboard log --as <seat> [--title "…"] (<text> |
   --stdin)` appends:
   ```
   ##### <SEAT-UPPERCASED> <ISO>: <title or first line>

   <text verbatim>

   ```
   creating the file with `# Log — YYYY-MM-DD` on line 1 if absent. Append-only: core exposes no
   rewrite. `repoboard log show [--date YYYY-MM-DD] [--seat s]` prints it.
3. **`repoboard init --practices`** — after the normal `init` (or on a repo that already has a board),
   creates if absent: `STATE.md` (sections with a one-line placeholder each, stamped now), today's log
   file, `leases.yml` (`leases: []`, `windows: []`), and **`NEXT-AGENT-PROMPT.md`
   at the REPO ROOT** with exactly these eight lines (adapt paths to `.repoboard/`):
   1 read CLAUDE.md · 2 read `.repoboard/STATE.md` (stamp older than the newest log → the log wins) ·
   3 read today's and yesterday's log · 4 read decided cards (`repoboard card list --json`, the `decision` block) before asking the owner
   anything — a decided card is authority · 5 the queue is the board (`repoboard card list --status todo`)
   · 6 write your block with `repoboard log --as <seat>` as you go · 7 rewrite STATE when you stand down
   · 8 `repoboard check` before you start and before you stop.
   **Never overwrites an existing file** (plan O9's standing constraint) — prints `kept <path>` for each.
4. **`repoboard check [--json] [--budget <bytes>]`** — exit 1 with one line per finding, exit 0 with
   `ok` when none:
   - `stale-state`: STATE stamp older than the newest log file's last modification (mtime) or newest
     `##### ` header time inside it, whichever is later.
   - `active-without-lease`: a card in an `active: true` column whose `assignee` holds no live lease on
     ANY resource (a seat working with nothing held). Warning-grade: print, still exit 1 only if
     `--strict`.
   - `stale-lease`: any lease past `until` (P8.2's `staleLeases`).
   - `needs-decision`: count of cards with an unanswered `decision` (informational, never fails).
   - `cost-over-budget`: P8.4 lands after this; **stub the finding behind a function `costFinding()`
     that returns `null` for now and leave a `// P8.4` comment** — P8.4 fills it. Do not build cost here.
5. **Core (pure, `packages/core/src/state.ts`, `log.ts` already exists for card logs — add `repolog.ts`):**
   `parseState(text) → {stamp, sections}`, `renderState(sections, openDecisions, {now, actor})`,
   `setStateSection(...)`, `formatLogBlock({seat, ts, title, text})`, `appendLogBlock(text, block)`,
   `parseLogBlocks(text)`, `checkFindings({state, logs, cards, config, leases, now}) →
   Finding[]`. Purity stays green.
6. **Server:** store watches `STATE.md` and `log/`; `GET /api/state` (rendered), `PUT
   /api/state/section`, `GET /api/log?date=`, `POST /api/log`, `GET /api/check`. WS snapshot carries
   `state` and today's `log`. MCP `get_state`, `set_state_section`, `append_repo_log`, `check`.
7. **Web:** STATE rendered as a collapsible panel at the top of the Board view (collapsed state
   remembered in localStorage); OWNER QUEUE inside it links to the Decisions tab; the daily log as a
   **Log** timeline beside the Ticker (newest block first, seat avatar, title, expand for text).
8. **Bytes (O3):** `repoboard state` output on the fpj-shaped fixture; `log show` for a 3-block day;
   MCP `get_state` result; schema growth. Into `docs/AGENTS.md` §10 "State, log, check" and your §7.

## Tests (each watched to FAIL once)

core: state parse/render round-trip with generated OWNER QUEUE; set-section restamps and touches
nothing else (byte compare of other sections); log block format + append-only; parseLogBlocks on a
3-seat day; `checkFindings` each finding + the `ok` case. server: `init --practices` on (a) an empty
dir, (b) a dir with an existing NEXT-AGENT-PROMPT.md and STATE.md → both `kept`, byte-identical
after; `log` CLI creates then appends; `check` exit codes 0/1; HTTP/MCP round-trips. web: STATE panel
renders sections and the generated queue; Log timeline orders newest first.

## Controls — demanded, VERIFIED (perturb → read back → typecheck → the named test fails → `cp` restore → `diff` empty)

- C1 make `init --practices` OVERWRITE an existing STATE.md → the `kept` test must fail with the
  byte-compare. **This is the one that guards the owner's README-class files.**
- C2 make `stale-state` compare against the log file's CREATION time instead of the newest block →
  the test where a block is appended later must fail.
- C3 make `repoboard log` rewrite the file with only the new block → the append test fails.

## Definition of done

`pnpm test` ×2 (both counts), typecheck 0, lint 0, build; AGENTS.md §10 with bytes; README row; plan
P8.3 `**Landed <sha>**`; one commit by named path (never `RCB-31/32/33/35`); `RCB-38` → review. No push.

## §7 — your log
