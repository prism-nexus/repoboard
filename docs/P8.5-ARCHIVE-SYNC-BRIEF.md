# P8.5 — `archive` done cards; `sync-issues` from a README K-list (brief, 2026-09-17)

Card: `RCB-40`. Plan: `docs/BUILD-PLAN.md` P8.5 and §11 O9. Builder subagent in
`/Users/hometown/Projects/Repos/Remember-Connect-Build` (no remote). **Starts AFTER P8.4 has landed.**
Read the four earlier P8 briefs' §7 logs, `CLAUDE.md`, plan §0–§2 and P8, `docs/AGENTS.md`,
`/Users/hometown/Projects/Repos/freshpickedjobs/docs/VERIFICATION-SPECIES.md` — **species 6 and the E1
lesson matter here**: a text rule for "a K-entry" landed on the wrong block twice in that repo on
2026-09-16/17. `RCB-40` → doing first; leave it in `doing` with a `verified: …` log line when done (O11: no review column; the orchestrator moves it to `done`).

## Why this exists (measured)

freshpickedjobs' README §Known issues held 68 open and 59 closed `K<n>` entries struck in place in
3,379 lines; a fresh seat could not read the queue. On 2026-09-17 the closed ones were moved out by a
script (63 entries, byte-identical). The board should be the VIEW of that list without a second copy of
any entry's text (O5), and done cards should leave the board the same way closed entries left the README.

## Locked decisions (build them; CORRECT in §7 with the measurement if one is wrong)

1. **`repoboard archive [--older-than <14d|Nh|ISO>] [--dry-run] [--as actor]`** — for every card whose
   `status` is a `done: true` column AND `updated` is older than the cutoff (default `14d`), move the
   file `.repoboard/cards/<id>.md` → `.repoboard/archive/<id>.md`: `git mv` when `.repoboard/` is inside
   a git work tree and the file is tracked, else `fs.rename`. **Byte-identical file** (no rewrite, no
   log line appended — the card is history now). One `type: 'archive'` event per card. The store does
   NOT load `archive/`; `card show <id>` on an archived id says `archived: .repoboard/archive/<id>.md`.
   `--dry-run` prints the list and touches nothing (prove it).
2. **`repoboard sync-issues <path>#<heading> [--status todo] [--label issue] [--dry-run] [--as actor]`**
   — reads a markdown file (repo-relative; `..`/absolute refused) and the section under the first
   heading whose text starts with `<heading>` (reuse `refs.ts`'s heading rule EXACTLY — same function,
   so the two agree). Within that section, an **issue item** is a list item whose first line begins at
   column 0 with `- **K<n>` (open) or `- ~~**K<n>` (struck = closed). Nothing else is an item (the E1
   near-miss `- ~~- **K` is NOT an item; report such lines as `skipped: malformed strike` in the output
   and your §7). For each open item with no card whose `refs` contains `<path>@K<n>`: create a card —
   `title` = `K<n> ` + the first line's text after `**K<n>` up to the first `**` or `—` or ` — `,
   truncated at 100 chars; `status` per flag; `labels: [issue]`; `refs: [<path>@K<n>]`; body
   `Filed from <path> §<heading>. The entry is the text; this card is the pointer.` For each struck or
   vanished item whose card exists and is not in a `done` column: move it to the first `done: true`
   column with log `synced: entry closed in <path>`. **Idempotent by ref** — a second run creates and
   moves nothing (test it). **NEVER writes `<path>`** (plan O9's standing constraint; prove with a byte
   compare before/after).
3. **Core (pure, `packages/core/src/issues.ts`):** `parseIssueItems(sectionText) → {items:[{n, open,
   firstLine, title}], malformed:[line]}`, `planSync(items, cards, {path, status, doneColumn}) →
   {create:[…], close:[…], unchanged:n}`. `archive` gets `selectArchivable(cards, config, cutoff)`.
   Purity green.
4. **MCP:** `archive_cards` (`olderThan?`, `dryRun?`), `sync_issues` (`path`, `heading`, `dryRun?`).
   **HTTP:** `POST /api/archive`, `POST /api/sync-issues`. Web: the Board's `done` column header gets an
   "archive older than 14d" action that calls the endpoint and reports the count; nothing else.
5. **Bytes (O3):** `sync-issues --dry-run` output on the fpj README (see 6); MCP schema growth. Into
   `docs/AGENTS.md` §12 "Archive and issue sync" and your §7.
6. **Measure on freshpickedjobs READ-ONLY:** `sync-issues README.md#Known issues --dry-run --root
   /Users/hometown/Projects/Repos/freshpickedjobs` must report **68 to create, 0 to close, 0 malformed**
   (the state after E1 on 2026-09-17 — if it reports anything else, one of us is wrong; measure, do not
   assume, and write both numbers in §7). Prove nothing was written: `git -C <fpj> status --short`
   identical before and after, and `.repoboard/` absent there. **Do NOT run without `--dry-run` on
   freshpickedjobs** — adoption is the orchestrator's step, on the owner's word.

## Tests (each watched to FAIL once)

core: `parseIssueItems` on a fixture that includes an open entry, a struck entry, a `- ~~- **K`
near-miss, a `- **K` inside prose (not column 0), a non-K bullet, and a `Closes K<n>` sentence inside an
open entry — expected list written BY HAND first; `planSync` create/close/idempotent; `selectArchivable`
boundary (`updated` == cutoff stays). server: `archive` moves byte-identical (compare hashes), `git mv`
path vs rename path (two fixtures), `--dry-run` touches nothing, store no longer lists the card, `card
show` says archived; `sync-issues` creates with the exact title/refs/body, closes on strike, idempotent
second run, source file byte-identical, refuses `..`; MCP/HTTP.

## Controls — demanded, VERIFIED

- C1 make the item rule `startsWith('- ')` + contains `**K` → the near-miss and the prose-line cases
  must fail (this is species 6, exactly).
- C2 make `sync-issues` append a marker line to the source file → the byte-identical test fails.
  **This is the control that guards the README.**
- C3 make `archive` rewrite the card through `serializeCard` → the hash test fails.

## Definition of done

`pnpm test` ×2, typecheck 0, lint 0, build; AGENTS.md §12 with the fpj dry-run numbers; README row;
plan P8.5 `**Landed <sha>**` and the P8 exit criterion's first clause checked against the dry-run; one
commit by named path (never `RCB-31/32/33/35`); `RCB-40` stays in `doing` with its `verified:` log line; the orchestrator moves it to `done`. No push.

## §7 — your log
