{
  "name": "@repoboard/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "repoboard domain logic: cards, board config, transitions, presence. I/O-free.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit && tsc -p tsconfig.build.json --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "dev": "echo '@repoboard/core: nothing to run (library)'"
  },
  "dependencies": {
    "yaml": "^2.9.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.1"
  }
}
