// @ts-nocheck — fixture text only, read as a string by the detector; `vite` is not a dependency
// of this package.
import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    port: 5174,
  },
});
