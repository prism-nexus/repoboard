{
  "name": "@rcb/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "rcb server: CLI, HTTP, WebSocket, file watcher, repo scanner, MCP. Stub until P2.",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "dev": "echo '@rcb/server: dev server not implemented yet (P2.1)'"
  },
  "devDependencies": {
    "@types/node": "^26.4.1"
  },
  "dependencies": {
    "@rcb/core": "workspace:*"
  }
}
